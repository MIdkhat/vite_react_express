{
  "dependencies": {
    "concurrently": "^7.0.0"
  },
  "type": "module",
  "scripts": {
    "start": "concurrently \"cd ./server && npm run server\" \"cd ./client && npm run dev\"",
    "build": "cd ./client && vite build"
  }
}
